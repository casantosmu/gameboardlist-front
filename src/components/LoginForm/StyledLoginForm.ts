import styled from "styled-components";

const StyledLoginForm = styled.form`
  .login-form__checkbox-field {
    text-align: center;
    padding-bottom: 1rem;
  }

  .login-form__button-field {
    display: flex;
    justify-content: center;
  }
`;

export default StyledLoginForm;
